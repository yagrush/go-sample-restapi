{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "[app] Launch and Debug",
      "type": "go",
      "request": "attach",
      "mode": "remote",
      // "mode": "test",
      // "args": ["-test.v", "-test.run", "TestYourFunc"],
      "substitutePath": [{ "from": "${workspaceRoot}/app", "to": "/app" }],
      "cwd": "${workspaceRoot}/app",
      "port": 2349,
      "host": "localhost",
      "dlvFlags": []
    }
  ]
}
